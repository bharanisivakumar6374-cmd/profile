class EmailService{constructor(){this.emailLog=this.loadEmailLog();this.emailTemplates=this.initializeTemplates()}loadEmailLog(){return JSON.parse(localStorage.getItem('lms_email_log')||'[]')}saveEmailLog(){localStorage.setItem('lms_email_log',JSON.stringify(this.emailLog))}initializeTemplates(){return{enrollment:this.getEnrollmentTemplate(),welcome:this.getWelcomeTemplate()}}getEnrollmentTemplate(){return{subject:'Welcome to {courseName}!',body:'<h2>Welcome to {courseName}!</h2><p>Hi {studentName},</p><p>Enrolled successfully in <strong>{courseName}</strong>.</p><p>Instructor: {instructorName}</p><p>Duration: {duration}</p><p>Start: {startDate}</p><p><a href="{zoomLink}">Join Class</a></p>'}}getWelcomeTemplate(){return{subject:'Welcome to EduFlow!',body:'<h2>Welcome to EduFlow!</h2><p>Hi {userName},</p><p>Welcome to the Smart Learning Management System!</p><p>Role: {role}</p>'}}sendEmail(to,templateName,variables){const template=this.emailTemplates[templateName];if(!template){console.error(`Template not found: ${templateName}`);return false}let subject=template.subject;let body=template.body;for(const[key,value]of Object.entries(variables)){subject=subject.replace(new RegExp(`{${key}}`,'g'),value);body=body.replace(new RegExp(`{${key}}`,'g'),value)}const email={id:'email-'+Date.now(),to:to,subject:subject,body:body,template:templateName,status:'sent',timestamp:new Date().toISOString()};this.emailLog.push(email);this.saveEmailLog();console.log(`📧 Email sent to ${to}: ${subject}`);return email}sendEnrollmentEmail(student,course){const assignments=db.getAssignmentsByCourse(course.id);const lastAssignment=assignments.length>0?assignments[assignments.length-1].dueDate:'TBA';return this.sendEmail(student.email,'enrollment',{studentName:student.name,courseName:course.title,instructorName:course.instructor,duration:course.duration,startDate:course.startDate,zoomLink:course.zoomLink})}sendWelcomeEmail(user){return this.sendEmail(user.email,'welcome',{userName:user.name,role:user.role})}getEmailLog(){return this.emailLog}}const emailService=new EmailService();